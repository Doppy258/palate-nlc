/** Seed data used only by `npm run db:seed` — not imported at runtime by the API. */
export {
  BADGES,
  FRIENDS,
  INITIAL_USER,
  LEVELS,
  QUESTS,
  RESTAURANTS,
  SEED_BITES,
  SEED_USER_BITES,
} from '../src/data/seed.ts'
